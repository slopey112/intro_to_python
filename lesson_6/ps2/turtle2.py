import turtle

distance = 100
t = turtle.Pen()
y = turtle.Pen()

while True:
    move = input()
    # Mistakes in the code start here.
    if move == 'w'
        t.setheading(90)
        t.forward(distance)
        y.setheading(270)
        y.forward(distance)
    elif move = 's':
        t.setheading(270)
        t.forward(distance)
        y.setheading(90)
        y.forward(distance)
    elif move == a:
        t.setheading(180)
        t.forward(distance)
        y.setheading(0)
        y.forward(distance)
    else move == 'd':
        t.setheading(0)
        t.forward(distance)
        y.setheading(180)
        y.forward(distance)
